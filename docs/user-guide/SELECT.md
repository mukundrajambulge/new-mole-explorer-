# Select

Open **Select** in the rail, choose **Select all**, or enter a selection in the console (`select polymer`, `select chain A`, `select ligand`). **Clear selection** and `Esc` call the same canonical clear action. Selection overlays are presentation-only; they do not change atom radii, colors, or representations.
