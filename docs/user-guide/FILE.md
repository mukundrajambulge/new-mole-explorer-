# File

**Open/Import** loads one admitted coordinate file. **Add Structure** appends another object without replacing existing objects. **Fetch** retrieves an official RCSB mmCIF by explicit PDB ID. **Save/Load Session** and **Export** preserve or emit the supported project state through the existing lifecycle controls.

Expected result: the object appears in Objects & Selections, source provenance is shown, and the canvas renders its coordinates.
