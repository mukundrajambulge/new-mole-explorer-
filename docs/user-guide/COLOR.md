# Color

Open **Color**, choose a target scope, then choose a registered scheme or explicit color. With an active selection, the selection scope is available so only those atoms receive the override. `color inherit, <selection>` clears the explicit override.
